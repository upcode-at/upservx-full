services:
  openvpn:
    image: kylemanna/openvpn:2.4
    restart: unless-stopped
    ports:
      - "1194:1194/udp"
    cap_add:
      - NET_ADMIN
    volumes:
      - ${APP_DATA_DIR:?APP_DATA_DIR is required}/openvpn:/etc/openvpn
